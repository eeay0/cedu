#include <stdio.h>  // Directive

int main(void) {    // Function definition the body of the function is a statement "{...}"
    printf("Parkinson's Law:\nWork expands as to ");  // Statement
    printf("fill the time\n");                        // Statement
    printf("available for its completion.\n");        // Statement
    return 0;                                         // Statement
}
